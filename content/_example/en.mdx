# Example content document

This is a placeholder demonstrating the `content/<slug>/<locale>.mdx` convention
(see `content/README.md`). Delete `content/_example/` once real content exists.

The build guard (`scripts/check-content.mjs`) requires this slug to also have an
`es.mdx` sibling — remove one to see the build fail.
