import type { ContactProfile } from "../../src/lib/profile/types";

const contact: ContactProfile = {
  notice: "SAMPLE CONTACT DATA — replace email and social links before publishing.",
  email: "hello@example.com",
  github: null,
  linkedin: null,
};

export default contact;
