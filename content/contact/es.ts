import type { ContactProfile } from "../../src/lib/profile/types";

const contact: ContactProfile = {
  notice: "DATOS DE CONTACTO DE EJEMPLO — reemplaza el correo y los enlaces sociales antes de publicar.",
  email: "hello@example.com",
  github: null,
  linkedin: null,
};

export default contact;
